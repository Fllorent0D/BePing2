import { AppearDirective } from './appear.directive';

describe('AppearDirective', () => {
  it('should create an instance', () => {
    const directive = new AppearDirective();
    expect(directive).toBeTruthy();
  });
});
