import { FadeHeaderDirective } from './fade-header.directive';

describe('FadeHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
